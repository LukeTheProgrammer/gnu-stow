# Global preferences

Always-on rules that apply to every project, on every machine.

Keep this file short. Every line here is loaded into context on every turn of every
session; a long file gets skimmed rather than followed. If a preference needs
examples or has exceptions, it belongs in a skill under `skills/`, not here.

## Rules

<!-- One line each, imperative, no hedging. Nothing here yet. -->
